{# templates/jalon/edit.html.twig #}
{% extends 'base.html.twig' %}


{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('build/edit.css') }}">
{% endblock %}

{% block title %}Modifier le Jalon{% endblock %}

{% block body %}
    <h1>Modifier le Jalon</h1>

    {{ form_start(form) }}
        {{ form_row(form.nom) }}
        {{ form_row(form.dateEcheance) }}
        {{ form_row(form.statut) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('jalon_index') }}">Retour à la liste</a>
{% endblock %}
