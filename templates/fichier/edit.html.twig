{% extends 'base.html.twig' %}

{% block title %}Modifier Fichier{% endblock %}

{% block body %}
    <h1>Modifier Fichier</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('fichier_index') }}">Retour à la liste</a>
    {{ include('fichier/delete.html.twig') }}
{% endblock %}
